/**
 * <b>Capa de presentación</b> (arquitectura en capas).
 * <p>
 * Contiene los componentes que interactúan directamente con el usuario:
 * entrada por consola, salida por pantalla, menús. Solo se encarga de
 * capturar datos y mostrar resultados; delega la lógica en la capa de aplicación.
 */

package proyectowallet.presentation;
